@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Account/Logout">
        ¡Bienvenido @User.Identity.Name!
        <button type="submit" class="m1-2 btn btn-link">Salir</button>
    </form>
}

else
{

}